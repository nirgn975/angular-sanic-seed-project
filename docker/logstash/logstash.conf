input {
	tcp {
		port => 5959
    codec => json
	}
}

filter {
  grok {
    match => { "message" => "logstash:%{WORD:name}" }
  }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
}
